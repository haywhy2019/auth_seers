"use client";
import { Add } from "@carbon/icons-react";
import { Button } from "@carbon/react";

const Home = () => {
  return (
    <div>
      <Add size="24" />
      <Button>Login</Button>
    </div>
  );
};

export default Home;
